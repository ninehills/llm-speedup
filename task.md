先制定plan，然后完成，注意每一步都要check。

1. 请参考 Quantization.md 的 2.2 和 2.3 对Qwen3-4B 前后量化后的模型进行效果测试（主要考虑vllm需要在后台启动和管理）
效果测试前需要用curl发10个请求预热vllm引擎。

2. 从测试日志中提取所有math500、iquiz的 Prompt和Response、是否正确、消耗时间。

3. 构造一个网页，直观的展示量化前后模型在math500、iquiz上的效果对比。参考影片
MMMU_v2.mp4 。
    - 注意虽然我们实际请求是并发请求的，但是表现形式还是串行的去请求。
    - math500 和 iquiz 分别只选100道，通过tab的方式切换演示。
    - 参考视频，一开始是真实时间，然后逐渐加速，在30s内完成单个数据集的演示。
    - 在最后显示平均 tokens 输出速度，单位是 tokens/s。

1. 用  frontend-design 重构网页让其高雅大方，完美还原 MMMU_v2.mp4 的效果（包括但不限于字体、颜色、布局、交互等）。模型没有读视频能力的话可以用ffmpeg将视频抽取关键帧图片。
2. 重新进行测试，进行单线程测试，从而最好的展示量化前后的速度。